<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.srizan.dinlipi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReminderEditorActivity"></activity>
        <activity
            android:name=".NoteActivity"
            android:label="Notes" />
        <activity
            android:name=".VoiceNotesActivity"
            android:label="Voice Notes" />
        <activity
            android:name=".VoiceRecorderActivity"
            android:label="Voice Recorder" />
        <activity
            android:name=".ReminderActivity"
            android:label="Reminders" />
        <activity
            android:name=".NoteEditorActivity"
            android:label="Note Editor" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.NoteProvider"
            android:authorities="com.srizan.dinlipi"
            android:exported="false" />
    </application>

</manifest>